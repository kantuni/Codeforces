#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,m;
    cin>>n>>m;int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    int k=0;
    for(int i=1;i<n;i++){
        k++;
        if(abs(arr[i]-arr[i-1])>m){k=0;}
    }
    cout<<(k+1)<<endl;
}
